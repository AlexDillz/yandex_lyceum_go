// Напишите функцию ConcatenateStrings(str1, str2 string) string,
// которая принимает две строки и склеивает их вместе, разделяя пробелом.

package main

func ConcatenateStrings(str1, str2 string) string {

	length := str1 + " " + str2

	return length

}
